## Implementation details

**Note**: this implementation of Jack compiler is different from the one proposed in the course, to be shorter and faster to write. Though main idea is the same, of course.

 * `lexer.py` - another name for a tokenizer. Scanner, lexer, tokenizer mean basically the same thing.