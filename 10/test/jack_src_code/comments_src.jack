let a = 2;  // Inline comment


/* One-line regular comment. */
/** One-line API comment. */
/*
    Multiline regular comment.
    1
    2
*/
/**
    Multiline API comment.
    3
    4
*/

method void incSize() {
    if (((y + size) < 254) & ((x + size) < 510) {
        do erase();  // Call eraser function
        let size = size + 2;
        do draw();
    }
    return;
}